-- This module will contain my multline line strings,
-- this an attempt to increase the readability.
return {
    Solutions = 
[[
Possible solutions towards the timeout error are:
- If one call consistently causes it: yield within the code or reduce the execution time
- If it happens randomly accross other benchmarks too: lower the `NoYieldTime`
]]
}